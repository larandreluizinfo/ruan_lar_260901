# AGENTS.md

## Regra obrigatória

- **Toda alteração** feita neste repositório deve ser **commitada e enviada (push)** para o repositório remoto.
- Sempre que concluir uma alteração, execute:
  - `git add -A`
  - `git commit -m "<mensagem descritiva>"`
  - `git push`
